import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.santiquiroz.lumina',
  appName: 'Lumina Calendar',
  webDir: 'dist',
  android: {
    backgroundColor: '#f8f9ff',
  },
};

export default config;
